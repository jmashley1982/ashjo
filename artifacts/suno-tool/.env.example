ANTHROPIC_API_KEY=
APP_PASSWORD=
FLASK_SECRET_KEY=
PORT=8082
BASE_PATH=/suno-tool
# Model is now chosen per-request in the app (Haiku/Sonnet radio buttons on the
# form). This only sets which one is preselected by default: "haiku" or "sonnet".
DEFAULT_MODEL_CHOICE=haiku
